# SQL Quiz

A series of tasks for practicing SQL.
From data definition (create table, set checks, constraints, foreign keys)
to queries (with unions, where having group by, order etc.).

Make sure to define tests that veirfy that scripts work as exepceted.
Use bunjs, keep it simple.

Create README.

src/
  DDL/
    task1.sql
    task2.sql
    task3.sql
    task4.sql
  DML/
    task1.sql
    task2.sql
    task3.sql
    task4.sql

spec/
